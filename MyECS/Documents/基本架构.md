# 基本架构

## 存储结构

因为大多数操作都是在询问一个 Entity 是否有某个 Component，或查询所有含有某个 Component 的所有实体，所以在 ECS 中，实际上 Entity 并不是主体，Component 才是主体。

这种情况下，就可以舍弃直觉上的让每个 Entity 存储它含有的 Component 的结构，而是让每个 Component 存储所有拥有它的 Entity。